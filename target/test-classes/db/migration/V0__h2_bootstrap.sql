-- Minimalny bootstrap dla testĂłw na H2
CREATE TABLE IF NOT EXISTS users (
                                     id BIGINT PRIMARY KEY AUTO_INCREMENT
);

CREATE TABLE IF NOT EXISTS tablice (
                                       id BIGINT PRIMARY KEY AUTO_INCREMENT
);
